func Merge (a,b)
	c = #{ }#
	g = []
	f = 1
	d = a.attrList()
	e = b.attrList()
	for attr in d 
		print (attr)
		if ( attr in e)
			if (type(a[attr]) == "Json" && type(b[attr]) == "Json")
				f = Merge(a,b)
			else
				c[attr] = a[attr]
				f = f + b[attr]
			end
		else
			f = a[attr]
		end
	end
	for attr in e
		if ( attr in d  )
			f = a[attr]
		end
	end
	print ("a")
	return f
end

func MergeUtil (a,b)
	if ( type(a) != "Json" || type(b) != "Json")
		print("Both the arguments must be JSON")
		return null
	end
	return Merge(a,b)
end

func main ()

a  = #{"name":"harsha"}#
b  = #{"name":"harsha"}#
MergeUtil (a,b)
print (a)
end